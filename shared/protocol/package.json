{
  "name": "@shared/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/schema.js"
  },
  "types": "./dist/schema.d.ts",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "dev": "sh -c 'tsc -p ./tsconfig.json -w --preserveWatchOutput'",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "~5.4.5",
    "rimraf": "^6.0.1"
  }
}
