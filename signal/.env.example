# Greyfall Signal Server — .env (production sample)

# Port (use either PORT or SIGNAL_PORT)
PORT=8787
# SIGNAL_PORT=8787

# Require JWT auth for /sessions (HTTP) and /ws (WebSocket)
SIGNAL_AUTH_REQUIRED=1

# Must match the app server's JWT_SECRET
JWT_SECRET=please-change-in-production

