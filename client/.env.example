# Greyfall Client (Vite) — .env (production sample)

# Google OAuth Client ID (required to enable Google login)
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# Optional: Absolute origin of the signal server
# If omitted, the app uses same-origin proxy under /api and /ws
# VITE_SIGNAL_SERVER_URL=https://signal.example.com

